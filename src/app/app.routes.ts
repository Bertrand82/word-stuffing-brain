import { Routes } from '@angular/router';
import { CallbackComponent } from './word-stuffing-root/callback-component/callback-component';

export const routes: Routes = [
   { path: 'callback', component: CallbackComponent },
];

